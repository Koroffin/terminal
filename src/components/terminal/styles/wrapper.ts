import styled from 'styled-components';
import { common } from './common';

export const Wrapper = styled.div`
  width: 100%;
  height: 100%;
  background-color: rgb(75, 79, 92);
  overflow: auto;
  font-family: monospace;
  padding: 15px 30px;
  ${common}
`;
